# Project Memory

- 2026-08-03 20:00 CEST — The repository ships project-level plugins from `.smallcode/plugins/`; these are loaded automatically before user-level plugins from `~/.config/smallcode/plugins/`.
- 2026-08-03 20:00 CEST — Gemma 4 must not receive serialized reasoning fields from earlier assistant messages. The bundled `gemma4-history-filter` pre-request hook removes `reasoning_content`, `reasoning`, and `reasoning_text` only for Gemma 4 model identifiers.
- 2026-08-03 20:10 CEST — Model-generated `bash` and `run` calls can omit their required `command`; the executor must return a structured tool error instead of calling string methods on `undefined` and crashing the TUI.
- 2026-08-03 20:20 CEST — `@file` and `@directory` prompt resolution already exists in `src/session/references.js`, but the fullscreen composer has no mention picker and the plugin API exposes no composer/key/input-completion hook. A clean extension implementation first needs a small core completion-provider API; prototype monkey-patching from a plugin is technically possible but unsupported and brittle.
- 2026-08-03 20:35 CEST — The plugin API now supports generic composer completion providers through manifest `completions` entries. The bundled `file-mentions` plugin owns `@` file/folder discovery; the fullscreen TUI owns picker rendering and keyboard behavior. Quoted references support paths containing spaces.
- 2026-08-03 20:45 CEST — Built-in plugins must live in the root `plugins/` directory, which npm packages. npm excludes `.smallcode/`, so that directory is only suitable for project-local plugins and examples.
- 2026-08-05 12:10 CEST — Thinking is controlled by `ThinkingState`. `/think` presets use 0/10/20/40/60/80/100% of the live max-output limit and the last chosen preset is stored in `~/.config/smallcode/thinking.json`; explicit legacy environment budget/disable values still override it. Provider request fields are applied centrally in `thinking_budget.js`.
- 2026-08-03 22:45 CEST — Thinking preset percentages resolve their maximum in this order: explicit `SMALLCODE_MAX_OUTPUT_TOKENS`, active Ollama model `num_predict` from `/api/show`, static 8192 fallback.
- 2026-08-03 23:20 CEST — The wide fullscreen TUI shows user messages, assistant responses, THINK, and DIFF chronologically in the large left pane; system/tool activity uses the compact right pane. Agent tool completions stream reasoning via a shared SSE accumulator; reasoning is removed before conversation/session persistence.
- 2026-08-03 23:50 CEST — Fullscreen user submissions are serialized through a queue. Busy/idle state is derived from queued/running agent jobs plus nested model streaming, preventing an older or inner stream from showing idle while work remains.
- 2026-08-05 11:21 CEST — Direct destructive shell commands are validated centrally against the fixed startup workspace for agent, API, persistent shell, and MCP paths. Outside, root-level, symlink-escaping, or ambiguous dynamic targets are blocked; executed programs remain outside this static command guarantee.
- 2026-08-05 11:51 CEST — Shell sessions have an explicit async close lifecycle and the full test suite exits without open handles. `/cancel` propagates through a session `AgentRunController`, model requests, asynchronous shell processes, and clears the fullscreen message queue.
- 2026-08-05 11:51 CEST — `/extensions` reports plugin/skill origins and diagnostics; `/reload` atomically refreshes both. Provider capabilities are probed non-blockingly, deeply cached by endpoint/model fingerprint, exposed via `/capabilities`, and used for streaming/tools/reasoning/output request choices.
- 2026-08-05 13:05 CEST — All model-facing file writes pass through pre-write candidate validation and an atomic commit. JS/TS, Python, YAML, CSS, JSON, and INI have guaranteed parsers; plugin manifests may add validators. Candidate code is parsed but never implicitly executed.
- 2026-08-05 14:10 CEST — SmallCode uses a persistent two-phase PLAN → EXECUTION lifecycle by default. PLAN exposes only explicitly read-only tools and a shell allowlist; `/execute` approves a stored workspace-scoped plan and every completion, failure, or cancellation returns to PLAN. Disable with `SMALLCODE_PLAN_MODE=false`, `[planning] enabled=false`, `--no-plan-mode`, or API `planning.enabled=false`.
- 2026-08-05 15:00 CEST — PLAN mode supports structured `request_user_input` clarification rounds. Fullscreen uses sequential option pickers with custom answers; Classic uses numbered prompts; CLI/API persist workspace-scoped pending question IDs for later continuation. The tool is unavailable in EXECUTION and DIRECT modes.
- 2026-08-05 15:25 CEST — Read-only PLAN analysis must end with a visible assistant answer or explicit model-connection failure; an empty response after successful reads triggers bounded completion retries. Safe stdout filters such as `sort`, `sed` (without `-i`), and `jq` are allowed while output-file forms remain blocked.
